env GOLINE=11
env GOPACKAGE=server
env GOFILE=main.go

! generate
stdout 'file main.go not found'

-- template.gohtml --
{{printf "Hello, %s!", "world"}}
-- go.mod --
module server

go 1.22
-- template.go --
package server

import (
	"embed"
	"html/template"
)

//go:embed *.gohtml
var formHTML embed.FS

//go:generate go run github.com/crhntr/muxt/cmd/muxt
var templates = template.New()