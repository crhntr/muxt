env GOLINE=9999
env GOPACKAGE=server
env GOFILE=template.go

! generate
stdout 'comment on line 9999 must be followed by a variable declaration'

-- template.gohtml --
{{printf "Hello, %s!", "world"}}
-- go.mod --
module server

go 1.22
-- template.go --
package server

import (
	"embed"
	"html/template"
)

//go:embed *.gohtml
var formHTML embed.FS

//go:generate go run github.com/crhntr/muxt/cmd/muxt
var templates = template.New()